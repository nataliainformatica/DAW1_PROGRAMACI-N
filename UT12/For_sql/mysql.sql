/*CREATE TABLE emp(
    empid INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    empname TEXT NOT NULL, 
    email TEXT NOT NULL,
    msg TEXT);
SELECT *  FROM emp;

INSERT INTO emp(empname,email)
VALUES ('SAM','TEST@test.com');
*/

SELECT *  FROM emp;